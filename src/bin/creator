#!/usr/bin/env python

import gtk
from os.path import normpath, dirname, abspath, realpath, join
from helpers.creator.creator import Creator

WHERE_AM_I = normpath(dirname(abspath(realpath(__file__))))

# Run standalone
if __name__ == '__main__':
    try:
        creator = Creator()
        gtk.main()
    except KeyboardInterrupt:
        pass
