#!/usr/bin/env python

import gtk
from os.path import normpath, dirname, abspath, realpath, join
from helpers.checker.checker import Checker

WHERE_AM_I = normpath(dirname(abspath(realpath(__file__))))

# Run standalone
if __name__ == '__main__':
    try:
        checker = Checker()
        gtk.main()
    except KeyboardInterrupt:
        pass
